NODE_ENV=development
DATABASE_URL=""


############## NETWORK CONFIGURATIONS #############
STELLAR_NETWORK=testnet
# comma-separated; empty = every contract in the registry for STELLAR_NETWORK
INDEX_CONTRACT_IDS=
# ingest attribution target; must be one of INDEX_CONTRACT_IDS; defaults to the registry's current
# production (mainnet) value: <MAINNET_V2_ID> — pending the mainnet v2 deployment
PROTOCOL_CONTRACT_ID="CA2QET2KOUGAECEVYQEQT3SLDDZRUMAQHI7MMDTFVJY62WTHUTERAUCD"
PRISMA_DEBUG=true

## Environment Variable for Integration Tests
HORIZON_API_URL=http://localhost:3001